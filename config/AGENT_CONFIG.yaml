project:
  name: "My Project"
  type: "backend" # frontend | backend | fullstack
  stack:
    - "python"

app:
  local_url: "http://127.0.0.1:4173"
  start_command: "python3 -m http.server 4173 -d demo_ui"

auth:
  enabled: true
  login_url: "/login"
  credentials:
    username: "demo"
    password: "demo123"

navigation_hints:
  - "After login go to dashboard"

improvement_level: 1 # 1=docs only | 2=docs+tests | 3=refactor
