project:
  name: "My Project"
  type: "backend" # frontend | backend | fullstack
  stack:
    - "python"

app:
  local_url: ""
  start_command: "python run.py --workspace . --phase phase1"

auth:
  enabled: false
  login_url: "/login"
  credentials:
    username: ""
    password: ""

navigation_hints:
  - "After login go to dashboard"

improvement_level: 1 # 1=docs only | 2=docs+tests | 3=refactor
